// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['LevelView', 'tmpl!rowDiv'], function(LevelView, rowDivTmpl) {
    var LayoutManager;
    return LayoutManager = (function(_super) {

      __extends(LayoutManager, _super);

      function LayoutManager() {
        return LayoutManager.__super__.constructor.apply(this, arguments);
      }

      LayoutManager.prototype.events = {
        'click .row-div': 'addRow'
      };

      LayoutManager.prototype.initialize = function() {
        this.layout = [
          {
            id: '123abc',
            cells: [
              {
                width: 4,
                widget: {
                  title: 'Missionary Badge'
                }
              }, {
                width: 8,
                widget: {
                  title: 'LMS Training'
                }
              }
            ]
          }, {
            id: '456def',
            title: 'Missionary Prep',
            cells: [
              {
                width: 4,
                widget: {
                  title: 'Immunizations'
                }
              }, {
                width: 4,
                widget: {
                  title: 'Dress and Grooming'
                }
              }, {
                width: 4,
                widget: {
                  title: 'Mormonorg'
                }
              }
            ]
          }
        ];
        return Backbone.Events.on('chooseNewLevelDividers', this.mkNewLevel, this);
      };

      LayoutManager.prototype.render = function() {
        var div, frag;
        frag = document.createDocumentFragment();
        div = rowDivTmpl();
        frag.appendChild($(div)[0]);
        _(this.layout).each(function(level, iter) {
          var levelView;
          levelView = new LevelView(level);
          frag.appendChild(levelView.render().el);
          return frag.appendChild($(div)[0]);
        });
        this.$el.html(frag);
        return this;
      };

      LayoutManager.prototype.addRow = function(evt) {
        var genUID, indx;
        genUID = function() {
          return ("0000" + (Math.random() * Math.pow(36, 4) << 0).toString(36)).substr(-4);
        };
        indx = this.$('.row-div').index($(evt.currentTarget));
        this.layout.splice(indx, 0, {
          id: genUID()
        });
        return this.render();
      };

      LayoutManager.prototype.mkNewLevel = function(widths, newRowId) {
        var emptyNewLevel, w;
        emptyNewLevel = _(this.layout).find(function(level) {
          return level.id === newRowId;
        });
        emptyNewLevel.cells = (function() {
          var _i, _len, _results;
          _results = [];
          for (_i = 0, _len = widths.length; _i < _len; _i++) {
            w = widths[_i];
            _results.push({
              width: w
            });
          }
          return _results;
        })();
        console.log(this.layout);
        return this.render();
      };

      return LayoutManager;

    })(Backbone.View);
  });

}).call(this);
